- [x] 测试环境服务器已成功在 `feature/add-phpwc-payment-channel` 启动并运行。
- [x] 测试 PID (199) 和 Key 成功写入到本地数据库的 `manager` 用户或全局配置中。
- [x] 系统能够成功生成正确的 PHPWC 支付链接，金额被正确重置为 `0.1`，并且签名（MD5）正确无误。
- [x] 系统能成功处理测试用例发送的伪造异步回调，正确完成 MD5 签名校验并记录支付成功日志。
- [x] 对应测试订单的数据库状态成功从 `pending` 变更为 `paid`。
- [x] 临时测试脚本已清理，本地预览 URL 已通过 OpenPreview 成功向用户提供。
